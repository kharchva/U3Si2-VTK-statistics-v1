[build-system]
requires = ["streamlit", "numpy", "matplotlib", "scipy", "setuptools", "wheel", "pybind11"]
build-backend = "setuptools.build_meta"
[tool.poetry.dependencies]
python = ">=3.12,<3.13"
[project]
requires-python = ">=3.12,<3.13"
